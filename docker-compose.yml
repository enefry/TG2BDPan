services:
  pan_saver:
    build: .
    container_name: pan_saver
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./tmp:/tmp/
      - ./log:/var/log
      - ./tg_data:/var/lib/telegram-bot-api
      - /media/data/service/data/workspace/mozilla:/data/mozilla
    extra_hosts:
      - "pcs.baidu.com:36.110.192.115"
      - "pcs.baidu.com:111.63.96.140"

  telegram-bot-api:
    image: aiogram/telegram-bot-api:latest
    container_name: telegram-bot-api
    restart: unless-stopped
    environment:
      TELEGRAM_API_ID: "${TG_API_ID:-}"
      TELEGRAM_API_HASH: "${TG_API_HASH:-}"
      TELEGRAM_STAT: 1
      TELEGRAM_LOCAL: 1
    volumes:
      - ./tg_data:/var/lib/telegram-bot-api
    ports:
      - "127.0.0.1:8081:8081"
